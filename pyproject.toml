[project]
name = "wellmanifest-repair-lifecycle"
version = "0.1.0.dev0"
description = "Wellmanifest autonomous repair lifecycle standard"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
